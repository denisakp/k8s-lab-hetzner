---
# Main infrastructure setup playbook
# Sets up universal infrastructure (packages, firewall, users, fail2ban)
# Does NOT include Kubernetes-specific configuration
#
# Usage:
#   ansible-playbook playbooks/site.yml                    # All VMs
#   ansible-playbook playbooks/site.yml --limit bastion    # Only bastion

- import_playbook: common.yml
